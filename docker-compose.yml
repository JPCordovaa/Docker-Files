version: '3.9'

services:
  node:
    image: node:20
    container_name: container-node
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "3000:3000"
    command: bash -c "npm install && npm run dev"
    volumes:
      - .:/app
      - /app/node_modules
